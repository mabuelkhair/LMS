<%= form_for(@create_course) do |f| %>
  <% if @create_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@create_course.errors.count, "error") %> prohibited this create_course from being saved:</h2>

      <ul>
      <% @create_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
