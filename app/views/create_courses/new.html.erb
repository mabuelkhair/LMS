<h1>New Create Course</h1>

    <div class="col-sm-4">
<%= form_tag "/create_courses", :name => "new_create_course" do %>

      <input type="text" class="form-control" name="create_course[name]" placeholder="Name">
      <input type="text" class="form-control" name="create_course[description]" placeholder="description">
      <input type="text" class="form-control" name="create_course[privacy]" placeholder="privacy">
      <input type="text" class="form-control" name="create_course[tags]" placeholder="fields of interests">
      <input type="text" class="form-control" name="create_course[objectives]" placeholder="learning objective">
    <input class="btn btn-primary btn-lg" type="submit" value="create">
<% end %>

	<a href="/create_courses">
		<button class="btn btn-primary btn-lg" >Back</button>
	</a>

	<% if @create_course.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@create_course.errors.count, "error") %> prohibited this article from being saved:</h2>
 
    <ul>
    <% @create_course.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

</div>
